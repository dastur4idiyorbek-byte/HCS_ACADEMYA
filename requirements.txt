# --- Telegram "tana" qatlami ---
aiogram==3.15.0

# --- Ma'lumotlar bazasi (SQLite -> PostgreSQL ga oson o'tish uchun) ---
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0

# --- Bozor ma'lumotlari ---
websockets==13.1
aiohttp==3.10.11

# --- Tahlil ---
# numpy/pandas ATAYLAB YO'Q: barcha indikatorlar sof Python'da yozilgan
# (sabab: docs/ARXITEKTURA.md, 9-bo'lim). Ular ~100 MB joy egallardi va
# Oracle Cloud bepul ARM serverida bu sezilarli.

# --- Signal grafigi rasmi ---
# Pillow — signal bilan birga ketadigan "uzun pozitsiya" grafigini
# chizish uchun. Bu yerda numpy/pandas dan farqli qaror: Pillow ~3 MB
# va u boshqa hech narsani tortmaydi.
#
# Nima uchun matplotlib EMAS: u numpy ni talab qiladi va ~50 MB — ya'ni
# yuqoridagi qoidani buzardi. Bizga esa kerak bo'lgani — to'rtburchak,
# chiziq va matn; buni Pillow o'zi qiladi.
#
# Nima uchun brauzer (Playwright) EMAS: ~400 MB Chromium va
# TradingView shartlariga zid avtomatlashtirish. Sabablar to'liq
# `bot/chart.py` boshida yozilgan.
Pillow==11.0.0

# --- Konfiguratsiya / vaqt ---
PyYAML==6.0.2
python-dotenv==1.0.1
tzdata==2024.2
